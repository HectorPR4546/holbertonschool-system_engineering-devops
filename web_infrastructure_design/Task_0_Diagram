```mermaid
flowchart TD
    User[User Browser] -->|Types www.foobar.com| DNS[DNS for foobar.com]
    DNS -->|A record returns 8.8.8.8| User
    User -->|HTTP over TCP| Nginx

    subgraph single_server [Single Server @ 8.8.8.8]
        Nginx[Nginx Web Server]
        App[Application Server]
        Files[Application Files]
        DB[(MySQL Database)]
        Nginx -->|Serve static files| Files
        Nginx -->|Proxy dynamic requests| App
        App -->|Read and write data| DB
    end

    Note[Single box = SPOF, downtime on deploys, no easy scaling]
    Nginx -.-> Note
```
