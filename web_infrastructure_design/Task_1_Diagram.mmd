flowchart TD
    User[User Browser] -->|HTTP request| LB[HAProxy Load Balancer]
    LB -->|Round-robin| ServerA
    LB -->|Round-robin| ServerB

    subgraph ServerAStack [Server A]
        ServerA[Nginx + App + MySQL\n(Primary)]
    end

    subgraph ServerBStack [Server B]
        ServerB[Nginx + App + MySQL\n(Replica)]
    end

    ServerA -. replication binlog .-> ServerB

    Note[SPOF: HAProxy + MySQL primary | No HTTPS | No monitoring]
    LB -.-> Note
